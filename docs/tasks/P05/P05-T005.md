# P05-T005: Add Emerald Proof 1 Local Configuration And Launcher

- **Task ID:** `P05-T005`
- **Phase:** `P05` - Live Gameplay Source (see [implementation plan](../../project/implementation-plan.md))
- **Status:** review
- **Owner:** Codex
- **Branch:** `feat/P05-T005-emerald-local-launcher`
- **Worktree/Clone Isolation:** Fresh independent clone `EOE-P05-local-launcher` alongside other task checkouts; no shared working tree was used.

## Objective

Provide a gitignored machine-local configuration and a Windows-friendly setup/launcher for the existing Emerald plus mGBA Proof 1 pipeline.

## Why This Task Exists

`P05-T001` through `P05-T004` completed the local source-to-overlay code path, but operators still had to reproduce several path assignments and directory commands manually. This task removes that setup friction without changing acquisition, mapping, normalized state, or overlay behavior.

## Dependencies

- `P05-T001` through `P05-T004`: completed on the starting `main@da2a0cb`.
- mGBA 0.10.3 and a legally obtained supported Emerald US Rev 0 ROM remain local operator dependencies.

## Scope

- `.env.local` convention with a public fake-path `.env.local.example`.
- Pure local env parsing, supported-variable selection, path resolution, numeric validation, and file/output validation.
- Output-directory preparation and mGBA child-process environment construction.
- `npm run proof:emerald` launcher with a non-launching `--check` mode.
- Manual savestate/Lua and mapper/server handoff instructions.
- Focused tests and configuration/adapter/project-memory documentation.

## Non-Goals

- No ROM, BIOS, save, savestate, credential, or machine-specific path is committed.
- No mGBA GUI automation, savestate/script CLI assumption, process supervisor, cloud/account dependency, source/mapping/schema change, or live acceptance claim.
- No events, actions, rules, maps, progress, multiplayer, Workbench, WebSocket, BizHawk, or hosted services.

## Acceptance Criteria

- Real `.env.local` is ignored and the committed example contains fake values only.
- Required mGBA/ROM/source/live paths and optional savestate/port/poll interval are handled deterministically.
- No process launches until required local files and output destinations validate.
- Source/live output parent directories are created.
- mGBA receives the configured ROM argument and source-snapshot environment.
- Manual savestate, Lua, mapper, server, and browser steps are printed clearly.
- Focused tests plus full repository validation pass.

## Implementation Notes

The launcher uses a narrow Node parser rather than adding `dotenv` or evaluating shell syntax. It supports comments, optional `export`, and quoted/unquoted literal values; it performs no variable expansion. Matching process-environment values override the file, and unrelated config keys are not copied into the structured proof config.

The launcher deliberately starts only mGBA. It does not manage long-running mapper/server children or automate mGBA UI actions. The spawned mGBA process inherits `EMERALD_SOURCE_SNAPSHOT_PATH`; commands printed for separate terminals explicitly set mapper/server variables because a child process cannot mutate a future terminal's environment.

No ADR was added: this is a scoped developer workflow over existing P05 contracts, not a platform/public-contract architecture decision.

## Files/Modules Expected

- `.env.local.example`, `.gitignore`, `.env.example`, `package.json`
- `tools/emerald-proof-config.mjs`
- `tools/launch-emerald-proof.mjs`
- `test/emerald-proof-config.test.js`
- Adapter README and applicable project/task memory.

## Tests Required

- Parser behavior and malformed-input rejection.
- Required fields, port, and poll-interval validation.
- Existing/missing executable, ROM, and optional savestate handling.
- Output-directory creation and output-path validation.
- mGBA ROM argument and child environment construction.
- Full `npm test`, Node syntax, launcher dry-run smoke, Markdown links, JSON parsing, `.gitignore`, and `git diff --check`.

Implementation results:

- `npm test`: 124/124 passing, including seven focused config/launcher tests.
- Node syntax checks passed for both new tools and the focused test file.
- `npm run proof:emerald -- --check --config <temporary-file>` validated fake local files, created both output directories, did not launch a process, and printed the configured PowerShell handoff.
- `.env.local` is ignored while `.env.local.example` remains trackable.
- Repository Markdown links, package JSON parsing, and `git diff --check` passed.

## Documentation Required

Root README, docs index/configuration, adapter README, code map, emulator matrix, feature inventory, roadmap, implementation plan, last-worked pointer, and this task record.

## Security/Compatibility Considerations

Config is parsed as data and never evaluated or interpolated. Required local files are read-only inputs to setup validation. The launcher does not inspect or copy ROM/save content. Output directories are created only after all configured paths validate. Existing `.env` usage remains available; `.env.local` is the focused Proof 1 convention.

The launcher passes the configured ROM as the sole mGBA argument and leaves optional savestate/script loading manual. Operators should close an existing mGBA process first so the launched process inherits the snapshot environment.

## Review Findings

Independent review is required because this task launches a configured executable and handles machine-local paths. Review is pending; merge approval is not granted.

## Result

Implemented the local config parser/validator and Proof 1 launcher without changing source, mapping, state, or overlay contracts. Real mGBA end-to-end acceptance remains pending.

## Branch And Merge Lifecycle

- **Implementation commit(s):** pending commit on this branch.
- **Fix commit(s):** none.
- **Merge approval status:** not requested.
- **Merge commit:** pending.
- **Post-merge validation:** pending.

## Completion Date


## Follow-Up Tasks

- Independently review this launcher/config boundary.
- Run and record the first real end-to-end mGBA smoke using the launcher.
- Scope broader P05 acquisition/mapping only after concrete smoke results.

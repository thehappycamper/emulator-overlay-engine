# P05-T013: Isolated Libretro Provider IPC Prototype

- **Task ID:** `P05-T013`
- **Phase:** `P05` — Live Gameplay Source (see [implementation plan](../../project/implementation-plan.md))
- **Status:** review
- **Owner:** Codex
- **Branch:** `feat/P05-T013-libretro-provider-ipc`
- **Worktree/Clone Isolation:** `C:\Users\Tayler Sorensen\EOE-P05-T008-libretro-provider` (independent clone from current `origin/main`)

## Objective

Promote the proven direct-host concept into the first isolated Libretro provider prototype. Native Libretro cores run in a child process while the EOE-side client uses a small versioned local IPC contract.

## Why This Task Exists

P05-T007 proved the direct-host fundamentals with a real Emerald Rev 0 session. The next risk is process isolation and a provider-neutral boundary, not another ABI feasibility experiment.

## Dependencies

P05-T007 completed and operator-verified; P05-T008 remains a separate unmerged memory-explorer branch and is not included.

## Scope

- Child-process native core execution under `experiments/libretro-provider/`.
- JSON-lines protocol for lifecycle, identity, capabilities, memory regions, bounded reads, frame execution, errors, and shutdown.
- Parent client timeout/exit handling.
- Generic memory access only; no Emerald/Pokemon semantics.

## Non-Goals

Writes, savestates, controller input, audio/video presentation, core updater, achievements, netplay, overlay integration, mapping, normalized state, and production frontend behavior.

## Acceptance Criteria

- Native core loading is confined to the child process.
- Normal startup/shutdown and structured lifecycle identity work.
- Child crash, unexpected exit, startup failure, malformed requests, ABI mismatch, out-of-range reads, and timeout behavior are deterministic.
- Memory reads are bounded and region-relative.
- Paths containing spaces cross the IPC boundary without shell interpretation.
- Parent tests remain alive after child failure.

## Implementation Notes

The child reuses the reviewed Libretro ABI declarations and address translation logic, with its own isolated Koffi dependency. A bootstrap frame runs during `initialize` because the official mGBA core publishes its memory map during frame execution. `read16` and `read32` are little-endian; range reads are capped at 1 MiB and returned as base64.

## Files/Modules Expected

- `experiments/libretro-provider/` — child, client, protocol, generic memory access, isolated package, and tests.
- `docs/decisions/0025-libretro-child-process-provider.md`
- project plan, emulator matrix, feature inventory, code map, and last-worked-on pointer.

## Tests Required

The provider suite must cover normal lifecycle, child failure, startup failure, malformed requests, out-of-range reads, ABI mismatch propagation, timeout/hang, paths with spaces, and parent survival. Full root validation remains required.

Implementation results: provider tests **8/8** passing; root `npm test` **207/207** passing with one pre-existing optional Koffi round-trip skipped because the direct-host experiment dependency is not installed at the root. Local child smoke against the available official mGBA core and Emerald ROM successfully returned core identity, EWRAM discovery (`0x02000000`, `0x40000`), a party-count read, frame execution, a second read, and clean shutdown. No ROM/core assets are committed. The fake child lives outside the test-discovery directory so the repository's normal `npm test` command remains finite.

## Documentation Required

ADR 0025, experiment README, `docs/project/implementation-plan.md`, `docs/project/last-worked-on.md`, `docs/project/features.md`, `docs/project/emulators.md`, and `docs/maps/code-map.md`.

## Security/Compatibility Considerations

The child-process boundary limits native crashes and hangs to the provider process, but it is not a full sandbox. IPC is local and unauthenticated. Koffi remains isolated from root production dependencies. No writes or arbitrary code execution are exposed through requests.

## Review Findings

Independent review required because this introduces native child-process execution and a public-facing provider boundary. Pending.

## Result

Implemented and pushed for independent review. This task establishes the process/IPC seam only; it does not make Libretro a production provider or close the broader P05 live-overlay acceptance gate.

## Branch And Merge Lifecycle

- **Implementation commit(s):** `ad0e996`.
- **Fix commit(s):** none affecting behavior; a governance-only renumbering commit resolves a task-ID collision (see note below).
- **Merge approval status:** not requested.
- **Merge commit:** pending.
- **Post-merge validation:** pending.

**Renumbering note:** this task was originally allocated `P05-T012` and its ADR was originally numbered `0023`. Both numbers were independently and concurrently allocated to a separate, unrelated task (the unified Emerald/BizHawk proof-session orchestrator, which used `P05-T012` and ADR `0023-proof-session-orchestrator.md`). Since the orchestrator branch was allocated first, this task was renumbered to `P05-T012` → `P05-T013` and its ADR `0023-libretro-child-process-provider.md` → `0025-libretro-child-process-provider.md` (0024 was independently claimed by a third concurrent task, `P03-T001`) as a governance-only fix. No implementation logic changed as part of this renumbering.

## Completion Date

Blank until approved integration.

## Follow-Up Tasks

- Provider-neutral Libretro source snapshot adapter after this IPC boundary is reviewed.
- Full live integration only after explicit acceptance of this provider contract.

# P00-T001: Initial Static MVP

- **Task ID:** `P00-T001`
- **Phase:** `P00` — Initial MVP (see [implementation plan](../../project/implementation-plan.md))
- **Status:** completed
- **Owner:** thehappycamper (with coding-agent assistance)

## Objective

Stand up the first shippable slice: a normalized Pokemon overlay state contract, a dependency-free static browser overlay, core damage/capture calculators, tests, CI, and the initial repository documentation scaffold (`AGENTS.md`, ADRs, knowledge base, code map).

## Why This Task Exists

Backfilled from git history to establish project history in `docs/tasks/`. This is the repository's first substantive commit and the origin of the project as "Pokemon Overlay Engine" before the domain-neutral pivot (see `docs/decisions/0011-rename-to-emulator-overlay-engine.md`).

## Dependencies

None — this is the starting point.

## Scope

Per `git show --stat ccc9642`: 40 files, 1912 insertions. Notably:
- `src/engine/{type-chart,damage,capture}.js` — Gen 3-style type effectiveness, damage range, and capture-chance calculators.
- `src/schemas/overlay-state.schema.json` — the normalized Pokemon overlay state contract.
- `src/overlay/app.js`, `public/{index.html,styles.css,sample-state.json}` — the static browser overlay.
- `test/engine.test.js` — calculator tests.
- `tools/dev-server.mjs` — dependency-free local dev server.
- `.github/workflows/ci.yml`, `.github/CODEOWNERS`, `.github/ISSUE_TEMPLATE/*`, `.github/pull_request_template.md` — CI and public contribution scaffolding.
- `AGENTS.md`, `README.md`, `CONTRIBUTING.md`, `SECURITY.md` — agent/human operating rules.
- `docs/decisions/0001` through `0004` — first four ADRs (normalized state API, documentation-as-memory, main-first branching, community-reviewed extensions).
- `docs/README.md`, `docs/maps/code-map.md`, `docs/knowledge/{architecture,domain-model,adapter-contracts}.md`, `docs/project/{community-extensions,emulators,features,last-worked-on,roadmap}.md`, `docs/graph/architecture.mmd` — documentation scaffold.
- `adapters/gen3-mgba/README.md`, `data/README.md`, `rulesets/README.md` — placeholder directories for future adapters/data/rulesets.

Two small follow-on commits closed out repository setup:
- `0f3c51c` "Record GitHub publish status" — trivial `last-worked-on.md` update.
- `1a050ce` "Initial commit" and `ab4b32f` (merge) — added the GitHub-created `LICENSE` file (MIT) into `main`.

## Non-Goals

- No live emulator export (explicitly deferred — the overlay reads a static `public/sample-state.json`).
- No extension/template model (added later in `P01-T001` and `P01-T002`).
- No mapping runtime (added later in `P01-T004`).

## Acceptance Criteria

- `npm test` passes (calculator tests).
- Static overlay renders sample state via `npm start`.
- CI runs tests on push/PR to `main`.

## Implementation Notes

This was authored in a single large commit rather than incrementally, per the evidence in `git log`. All work is dated 2026-08-08 in the repository's commit history.

## Files/Modules Expected

See Scope above; matches `git show --stat ccc9642` exactly.

## Tests Required

`test/engine.test.js` — type effectiveness, damage range, incoming-damage projection ranking, capture chance. Confirmed still passing as of `P01-T004`'s test run (5/5, later 39/39 once mapping tests were added).

## Documentation Required

Initial versions of all files listed under Scope — this task *created* the documentation checklist referenced by `AGENTS.md`, rather than following a pre-existing one.

## Security/Compatibility Considerations

None specific to this task; `SECURITY.md` and the ROM/BIOS/save-file public-safety policy were established here as repository-wide policy (later backed by `.gitignore` patterns in `P01-T004`).

## Review Findings

None recorded at the time. This task predates the review practice reflected later in `P01-T004`'s and `P01-T007`'s Review Findings sections.

## Result

Completed. Established the static MVP: normalized Pokemon state schema, calculators, overlay, tests, CI, and documentation scaffold, as the foundation all later phases build on.

## Commit(s)

- `ccc9642` — Initialize Pokemon overlay engine MVP
- `0f3c51c` — Record GitHub publish status
- `1a050ce` — Initial commit (LICENSE)
- `ab4b32f` — Merge remote-tracking branch 'origin/main' (LICENSE)

## Completion Date

2026-08-08

## Follow-Up Tasks

`P01-T001` (scalable extension model) built directly on this foundation.

# P04-T004: Restore Browser App-Shell Module Resolution

- **Task ID:** `P04-T004`
- **Phase:** `P04` — Event Routing And Actions
- **Status:** review
- **Owner:** Codex
- **Branch:** `fix/P04-T004-browser-module-resolution`
- **Worktree/Clone Isolation:** `C:\Users\Tayler Sorensen\EOE-P04-T004-ajv-fix-20260816`

## Objective

Make the real `public/index.html` app shell load through the existing development server and native browser ESM, including the dashboard and notification polling surfaces.

## Why This Task Exists

P04-T003 identified that the production shell failed on the bare `ajv/dist/2020.js` import before the browser could render the overlay. Node resolves the package through npm; a browser cannot resolve npm bare specifiers or execute Ajv's Node/CommonJS entrypoint in a native source-module graph.

## Scope

The Pokemon browser descriptor no longer imports semantic-event detector code. Presentation, state-contract metadata, and calculators remain browser-safe; Node-side event detection continues to import `src/events/derive.js` and Ajv through `src/domains/pokemon/events.js`. The full Node domain descriptor remains unchanged; the browser app resolves a separate browser-safe presentation descriptor.

## Non-Goals

No bundler, frontend framework, schema redesign, event/action behavior, notification feature, emulator integration, or P04-T004 successor work is included.

## Acceptance Criteria

- Real `public/index.html` loads through `tools/dev-server.mjs` without unresolved-module errors.
- Dashboard and independent notification polling render together.
- Notification text remains DOM-safe.
- Node-side event/schema validation and existing behavior remain intact.
- Clean dependency installation and full tests pass.

## Implementation Notes

The browser dependency chain was `app.js -> domains/index.js -> pokemon/index.js -> pokemon/events.js -> events/derive.js -> events/validate.js -> ajv/dist/2020.js`. The fix removes only the event-detector edge from the browser descriptor. The live Emerald orchestrator already imports `pokemon/events.js` directly, so the Node event pipeline remains explicit and unchanged.

## Files/Modules Expected

- `src/domains/pokemon/index.js`
- `src/domains/pokemon/events.js`
- `src/domains/browser.js`
- `src/domains/pokemon/presentation-descriptor.js`
- `test/browser-shell.test.js`
- related ADR/project documentation

## Tests Required

- `npm ci`
- `npm test`
- real Chrome smoke against `public/index.html`
- syntax, schema/JSON, Markdown-link, diff, and binary/NUL checks

## Documentation Required

ADR 0030, implementation plan, code map, and last-worked-on pointer.

## Security/Compatibility Considerations

This removes a Node-only dependency from the browser graph rather than shipping a second validator or weakening validation. `events.js` remains the Node-side validated detector module and continues to export the existing identity helpers.

## Review Findings

Independent review is required because this changes the browser/domain module boundary. No review findings yet.

## Result

Implemented and ready for independent review. The real app shell passed headless Chrome smoke with dashboard, live-state status, notification display, and escaped notification text.

## Branch And Merge Lifecycle

- **Implementation commit(s):** pending commit.
- **Fix commit(s):** none.
- **Merge approval status:** not requested.
- **Merge commit:** blank until merged.
- **Post-merge validation:** blank until merged.

## Completion Date

Blank — not yet completed.

## Follow-Up Tasks

- After independent review and integration, run the real one-command BizHawk faint-to-notification acceptance.
- Continue with the next explicitly scoped P04 task only after that acceptance is recorded.
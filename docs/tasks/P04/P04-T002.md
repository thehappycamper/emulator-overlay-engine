# P04-T002: Capability-Gated Action Executor

- **Task ID:** `P04-T002`
- **Phase:** `P04` — Event Routing And Actions (see [implementation plan](../../project/implementation-plan.md))
- **Status:** review
- **Owner:** Claude (governance/architecture-maintenance agent)
- **Branch:** `feat/P04-T002-capability-gated-action-executor`
- **Worktree/Clone Isolation:** Independent clone at `../EOE-P04-T002/`, cloned fresh from `origin/main` at `6298d61` (which already includes the merged `P04-T001` and `P05-T012`).

## Objective

Define the first capability-gated action execution boundary on top of merged `P04-T001`: a small, domain-neutral action-provider/executor interface that turns a validated `ActionRequest` into a real (but harmless) local effect, or a structured, fail-closed rejection - without ever letting rule evaluation itself perform a side effect.

## Why This Task Exists

`P04-T001`'s own recommended follow-up: "define a capability-gated action-provider boundary and a minimal in-memory executor for approved local actions. Keep provider mutation and permissions outside rule evaluation." See [ADR 0027](../../decisions/0027-capability-gated-action-executor.md).

## Dependencies

`P04-T001` (declarative rules producing validated action requests) complete and merged on `origin/main`.

## Scope

- New domain-neutral `src/actions/execute.js`: `createActionExecutor(providers, options)`, a four-stage fail-closed pipeline (supported → capability-granted → authorized → payload-valid → execute), returning a schema-validated `ActionResult` for every outcome, never throwing for request-shaped input.
- New `src/actions/schemas/action-result.schema.json` and `assertValidActionResult`/`ActionResultValidationError` added to `src/actions/validate.js`.
- Two harmless local action providers proving the architecture: `src/actions/providers/overlay-notification.js` (`overlay.notification`) and `src/actions/providers/system-log.js` (`system.log`), each with its own JSON Schema-validated payload contract and an injectable execution sink (defaults to a console line - no real I/O).
- Explicit, tested replay/duplicate-request semantics.

## Non-Goals

Memory writes, input injection, savestate operations, webhooks, Discord/Twitch/OBS integrations, scripting, multiplayer, or provider-specific emulator actions. `src/rules/evaluate.js` is not modified - rule evaluation remains completely side-effect-free and does not import this module.

## Acceptance Criteria

- A supported, capability-granted, authorized action with a valid payload executes and returns a schema-valid, `"executed"` result.
- An unsupported action type, a missing capability, an unauthorized action, and a malformed payload each fail closed with a distinct structured code, never executing the provider.
- A provider exception (sync or async) is returned as a structured `"failed"` result, never thrown from the executor.
- `executeAll()` processes a batch of requests strictly in array order (never concurrently).
- Duplicate/replayed action handling is explicitly defined and tested: an identical replay is not re-executed; a different request that happens to share the same idempotency key is never conflated with a cached result.
- The source `ActionRequest` is never mutated.
- Rule evaluation and action execution remain in separate modules with no import coupling in either direction.

## Implementation Notes

**Four-stage fail-closed pipeline.** Every `execute()` call passes through, in order: request-schema validity → provider registered for `actionType` (`UNSUPPORTED_ACTION_TYPE`) → `requiredCapability` present in the executor's granted-capability set (`MISSING_CAPABILITY`) → the provider's own `authorize(payload, context)` hook, if present (`UNAUTHORIZED_ACTION`) → the provider's own `validatePayload(payload)` (`INVALID_PAYLOAD`) → `execute(payload, context)`, wrapped in try/catch (`EXECUTION_FAILED` on throw). Every stage returns a fully schema-validated `ActionResult` rather than throwing, so a caller looping over many requests is never crashed by one of them.

**Capability decoupled from action type.** A provider declares `requiredCapability` as its own string, not required to equal `actionType`. The two proof providers happen to use similarly-named capabilities (`overlay.notify`, `system.log`), but the mechanism itself is capability-based, not action-type-based - independently verified by a test granting only one of the two capabilities and confirming the other provider's action is rejected `MISSING_CAPABILITY` even though its provider is registered. This is the seam a future, stronger-gated action (e.g. an emulator-write capability that is normally never granted) plugs into without any change to the executor framework or the rule engine.

**Authorization is a second, dynamic gate, deliberately distinct from the static capability grant.** Both proof providers require `context.sessionAuthorized === true` in their `authorize()` hook - a simple, uniform "this execution context may run local harmless actions" signal, independent of whether the executor was constructed with the underlying capability at all. A provider whose `authorize()` throws is treated as an authorization failure (`UNAUTHORIZED_ACTION`), not a crash.

**Replay/duplicate semantics, explicit by design.** Requests are keyed by `` `${correlationId}:${sequence}` `` - `action-request.schema.json` already documents `sequence` as unique within one evaluation run, so this is the smallest honest identity without inventing a new field. An identical replay (same `actionType` and payload under the same key) returns the original cached `ActionResult` with `status: "duplicate"` and does not invoke the provider again - proven by a test asserting the provider's own side-effect callback was invoked exactly once across two identical calls. A request that reuses the same key with a genuinely different payload is never conflated with the cached result; it is evaluated fresh on its own merits (proven by a dedicated test) - silently returning a stale result for a different action would be a correctness bug, not a convenience. Duplicate tracking is in-memory only, scoped to one executor instance's lifetime, and unbounded (disclosed limitation, not a concern for this proof).

**Fix round: single-flight, strengthened replay identity, and contained failures (see [ADR 0027](../../decisions/0027-capability-gated-action-executor.md)'s addendum for full rationale).** Three blocking gaps found before independent review were closed on this same branch:

- *Concurrent duplicate execution.* A second in-memory `inFlight` map (`Map<replayIdentity, Promise<ActionResult>>`) now single-flights concurrent identical requests: the first caller registers its pipeline promise synchronously (before any `await`), so JS's run-to-completion guarantees no concurrent caller can ever observe an empty cache and independently invoke the provider. Concurrent identical callers await that same promise and receive `duplicate` (on success) or the same non-success outcome (on failure/rejection) without a second invocation. The key is always freed in a `finally`, so a failed in-flight attempt can never deadlock the waiters or permanently poison the key - a later identical call always gets a fresh attempt.
- *Replay identity.* The identity moved from a bare `correlationId:sequence` pair to a full canonical fingerprint over `actionType`, `correlationId`, `sequence`, `ruleId`, `triggeringEvent`, and `payload` (a deterministic, key-order-independent serialization). Two requests are the same replay identity if and only if all six match; anything else - including a `ruleId` or `triggeringEvent` change under the same `correlationId:sequence` pair - is evaluated independently, never conflated as a duplicate and never rejected as a conflict.
- *Contained failures.* A provider returning a non-object value is now explicitly detected and converted to a structured `EXECUTION_FAILED` result. Every caller-supplied hook (`authorize()`, `validatePayload()`, `execute()`) now normalizes a thrown non-`Error` value (`null`, `undefined`, a string, a plain object) through a `describeThrown()` helper instead of reading `.message` on an untrusted value. `buildResult()` itself now has a try/catch backstop that converts an internally-invalid result shape into a guaranteed-schema-valid `INTERNAL_ERROR` failure rather than letting `ActionResultValidationError` escape `execute()`.

**Deterministic ordering.** `executeAll(requests)` is a plain `for` loop awaiting each `execute()` call in turn - proven with a provider whose `execute()` records start/end markers and includes an artificial delay on one request, confirming a slower earlier request always fully completes before a faster later one begins.

**Harmless proof providers.** `overlay.notification` (payload `{message: string}`) and `system.log` (payload `{level?: "info"|"warn"|"error", message: string}`) each deliver to an injectable sink (`context.notify`/`context.log`) that defaults to a `console.log` line - no file, network, or emulator I/O occurs unless a caller explicitly injects a sink that does that (which none of this task's code does).

## Files/Modules Expected

- `src/actions/execute.js` (new)
- `src/actions/schemas/action-result.schema.json` (new)
- `src/actions/validate.js` (extended: `assertValidActionResult`, `ActionResultValidationError`)
- `src/actions/providers/overlay-notification.js`, `system-log.js`, `index.js`, `schemas/overlay-notification-payload.schema.json`, `schemas/system-log-payload.schema.json` (new)
- `test/action-executor.test.js` (new)
- `docs/decisions/0027-capability-gated-action-executor.md` (new)

## Tests Required

Supported+authorized+valid execution; unsupported action type; missing capability (with a registered provider); unauthorized action (capability granted, authorization denied); an `authorize()` hook that throws; malformed payload (both providers, multiple malformation shapes); a genuinely malformed action request (not just payload); a provider `execute()` exception (sync and async); deterministic sequential `executeAll()` ordering; duplicate replay is not re-executed; a colliding-but-different request is not conflated with a cached result; source request immutability; `executeAll()` input validation and empty-array handling; malformed provider descriptor / duplicate provider registration rejection at construction; `supportedActionTypes()`/`grantedCapabilities()` introspection; independent per-provider capability gating; result-schema rejection of malformed shapes; platform-neutrality and no import coupling between rule evaluation and action execution in either direction; a full real P03-detector-event → rule → action-request → executor pipeline run.

Fix round additions: two concurrent identical requests invoke the provider exactly once (single-flight); two concurrent identical requests do not deadlock and are not poisoned when the in-flight execution fails, and the key is free for a fresh attempt immediately afterward; a sequential failure does not poison the replay key for a later identical retry; same `correlationId:sequence` with a changed `payload`/`ruleId`/`triggeringEvent`/`actionType` is independently executed rather than conflated as a duplicate, while a genuine full-field replay is still deduplicated; a provider returning a non-object value (string/number/boolean/array) is contained as a structured `EXECUTION_FAILED` result; `execute()`/`validatePayload()`/`authorize()` throwing `null`/`undefined`/a string/a number/a plain object never crashes `execute()` and always yields a schema-valid result.

Implementation results:

- `npm test`: **278/279 passing** (256 baseline on `origin/main` + 22 new tests in `test/action-executor.test.js`), 1 pre-existing skip unrelated to this task, 0 failures.
- `node --check` passed for every new/changed JavaScript file.
- All new JSON schema files parse and are exercised by Ajv2020 in strict mode.
- `git diff --check`: clean.
- Repository-wide relative Markdown link check: clean.

Fix round results (after rebasing onto `origin/main` at `9e723dc`, which brought in `P05-T013`'s merge):

- Added 6 new tests to `test/action-executor.test.js` (now 28 tests in that file): concurrent single-flight (asserting exactly one provider invocation), concurrent failure without deadlock/poisoning, sequential failure-then-retry, replay-identity strengthening (payload/ruleId/triggeringEvent/actionType each independently varied under the same `correlationId:sequence`), malformed non-object provider return, and non-`Error` thrown-value containment across `execute()`/`validatePayload()`/`authorize()`.
- `npm test`: **310/311 passing** (280 baseline on the rebased `origin/main` + 6 new tests, existing 22 unchanged and still passing), 1 pre-existing skip unrelated to this task, 0 failures. Confirmed stable across 3 consecutive full runs and 3 consecutive focused runs of `test/action-executor.test.js` (no flakiness in the concurrency tests).
- `node --check` passed for `src/actions/execute.js` and `test/action-executor.test.js`.
- `git diff --check`: clean. Repository-wide relative Markdown link check: clean (0 broken links across 115 Markdown files).
- Binary/NUL scan: clean for every changed file.
- Platform-coupling audit (`adapters/`, `domains/pokemon`, mGBA/BizHawk/Libretro, `process.platform`/OS-name literals): no matches in `src/actions/execute.js`.

## Documentation Required

`docs/decisions/0027-capability-gated-action-executor.md` (new ADR), `docs/maps/code-map.md`, `docs/project/implementation-plan.md` (`P04` section), `docs/project/last-worked-on.md`, this task record.

## Security/Compatibility Considerations

No arbitrary-code-execution path: providers are plain JavaScript objects supplied by the calling code at the source level, not loaded from user input, a manifest, or a rule/action-request payload - there is no new dynamic-evaluation surface. The executor is fail-closed at every stage: an unrecognized action type, an ungranted capability, a failed authorization check, or an invalid payload all refuse execution rather than degrading to a best-effort attempt. The two proof providers perform no real I/O by default (console-line only) and accept no path, network, or emulator-control input of any kind. This is additive-only: no existing schema, evaluator, or event contract changed.

## Review Findings

Pre-merge review scrutiny found three blocking issues, fixed on this same branch (see [ADR 0027](../../decisions/0027-capability-gated-action-executor.md)'s addendum and the Implementation Notes fix-round entry above for full detail):

1. Concurrent duplicate execution: two identical `execute()` calls in flight at the same time could each observe an empty replay cache and both invoke the provider. Fixed with an in-flight single-flight guard.
2. Replay identity was too weak: a bare `correlationId:sequence` pair could conflate requests that differed in `ruleId`, `triggeringEvent`, or `payload`. Fixed with a full canonical-fingerprint replay identity.
3. Provider/validator/authorization failures were not fully contained: a non-`Error` thrown value or a non-object provider return could produce an invalid or crash-prone outcome. Fixed with thrown-value normalization and an explicit malformed-return check, plus a `buildResult()` backstop.

Pushed for independent re-review; not yet reviewed or merged.

## Result

`src/actions/execute.js` provides a capability-gated executor completing the `event → rule → action request → executor` pipeline `P04-T001` deliberately stopped short of, proven against two harmless local action providers with independently-tested capability gating, dynamic authorization, fail-closed validation at every stage, deterministic ordering, and explicit, non-conflating replay semantics. Rule evaluation remains untouched and fully decoupled.

## Branch And Merge Lifecycle

- **Implementation commit(s):** `a07c742` (`Add capability-gated action executor (P04-T002)`).
- **Fix commit(s):** `b8fa224` (`Fix P04-T002 blocking findings: single-flight replay, stronger replay identity, contained failures`).
- **Merge approval status:** not requested — stopped for independent review per explicit instruction.
- **Merge commit:** blank until merged.
- **Post-merge validation:** blank until merged.

## Completion Date

Blank — not yet completed (merged + validated).

## Follow-Up Tasks

- A real overlay-integration provider (wiring `overlay.notification` to the actual browser overlay) once the presentation layer exposes a suitable local delivery channel - still local-only, no network/webhook work implied.
- Bounding/expiring the in-memory replay-tracking store if an executor instance is ever expected to run for an extended session.
- A capability-grant configuration format (who grants what, and how an operator reviews/changes it) - this task hard-codes grants at `createActionExecutor()` construction, which is sufficient to prove the architecture but not yet an operator-facing surface.
- Any provider representing a genuinely dangerous capability (memory writes, input injection, savestate control) remains a separate, later decision requiring its own authorization design - this task's contract supports it without changes, but does not implement or pre-approve it.

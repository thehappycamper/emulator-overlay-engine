# P01-T003: Rename To Emulator Overlay Engine; Template Schema

- **Task ID:** `P01-T003`
- **Phase:** `P01` — Platform Mapping Foundation (see [implementation plan](../../project/implementation-plan.md))
- **Status:** completed
- **Owner:** thehappycamper (with coding-agent assistance)

## Objective

Rename the project from "Pokemon Overlay Engine" to "Emulator Overlay Engine" / `EOE_` across active documentation and configuration, and add the template manifest schema (`src/schemas/template.schema.json`) plus a first example template.

## Why This Task Exists

Backfilled from git history. Recorded in `docs/decisions/0011-rename-to-emulator-overlay-engine.md`. Directly relevant to `docs/project/branding-and-renaming.md`, since this is the repository's one precedent for what a rename actually touches.

## Dependencies

`P01-T002` (template concept defined in ADR 0009) — this task gives it a schema.

## Scope

Per `git show --stat fe435dc` (18 files):
- `docs/decisions/0011-rename-to-emulator-overlay-engine.md` (new).
- `src/schemas/template.schema.json` (new), `examples/templates/pokemon-emerald-challenge/` (new).
- `.env.example` — variables renamed to the `EOE_` prefix.
- `.github/ISSUE_TEMPLATE/{feature_request,template_request}.md`, `.github/pull_request_template.md`.
- `AGENTS.md`, `CONTRIBUTING.md`, `README.md`, `SECURITY.md`, `adapters/gen3-mgba/README.md`.
- `docs/README.md`, `docs/decisions/0001-normalized-state-api.md` (naming reference update), `docs/graph/architecture.mmd`.
- `docs/knowledge/{adapter-contracts,architecture,custom-data-and-fusions,extension-workbench,shareable-templates}.md`.

## Non-Goals

Did not touch `package.json`'s repository-identity name in this diff scope beyond what's shown (package/repo slug alignment was part of the broader rename effort per ADR 0011's consequences, not itemized file-by-file here).

## Acceptance Criteria

Repository-wide grep for "Pokemon Overlay Engine," "POE_," and "pokemon-overlay-engine" returns no hits in active docs/config (verified in a later architecture audit of this repository — see `docs/project/last-worked-on.md` history and this task's Review Findings).

## Implementation Notes

n/a — backfilled record.

## Files/Modules Expected

See Scope.

## Tests Required

None added in this commit.

## Documentation Required

See Scope.

## Security/Compatibility Considerations

Environment variable prefix changed from an earlier form to `EOE_` — see `docs/project/branding-and-renaming.md` Category 3 for why this class of rename is compatibility-sensitive going forward (this task predates that document, so no deprecation window was documented for this specific change).

## Review Findings

A later independent architecture audit of this repository (2026-08-08, same day) explicitly grepped for stale `POE_`/`GOE_`/"Pokemon Overlay Engine"/"Game Overlay Engine" naming and old repository slugs, and found **no hits** — confirming this rename was fully and consistently executed across active documentation.

## Result

Completed. Rename executed cleanly (verified by independent audit, no stale references found); template schema established, later extended for extension-type reuse in `P01-T004`.

## Commit(s)

- `fe435dc` — Align docs and naming with emulator overlay engine

## Completion Date

2026-08-08

## Follow-Up Tasks

`docs/project/branding-and-renaming.md` generalizes the lessons of this task into a repeatable procedure for the next rename.

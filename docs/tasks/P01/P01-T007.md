# P01-T007: Harden JSON Pointer Array Writes

- **Task ID:** `P01-T007`
- **Phase:** `P01` — Platform Mapping Foundation (see [implementation plan](../../project/implementation-plan.md))
- **Status:** review
- **Owner:** Codex
- **Branch:** `fix/mapping-array-hardening`
- **Worktree/Clone Isolation:** Dedicated sibling worktree at `../EOE-P01-T007`; no working tree shared with another active agent.

## Objective

Close the sparse-array amplification denial-of-service found during adversarial security review of `P01-T004` (commit `13e926262b1685ccbcf232e834b6660a92485e4e`). Bound numeric array-index segments in `setJsonPointer` (`src/mapping/apply.js`) so a mapping project's `targetPath` cannot create arbitrarily large sparse arrays from a short input string.

## Why This Task Exists

`P01-T004`'s stated purpose is that mapping projects are safe to load from untrusted, community-authored data. Review found that claim did not hold for one input class: `setJsonPointer` places no upper bound on a numeric array-index path segment. A `targetPath` of `/arr/4000000000/y` (19 characters) instantly produces a JS array with `.length === 4000000001`. At a smaller, safely-testable scale (`/arr/10000000/y`), `JSON.stringify` of the resulting object alone took ~900ms and produced a 50MB string from a source object containing a single field — confirming this is a real amplification attack (tiny input, unbounded downstream cost via `JSON.stringify`, Ajv `items` validation, or array iteration), not a theoretical one. Full write-up: `P01-T004`'s Review Findings section.

## Dependencies

`P01-T004` (the mapping contract this hardens) — completed, this task is the one open follow-up before `P01` can close.

This task is the current blocker on the `P01` phase gate in the [implementation plan](../../project/implementation-plan.md) — `P01` cannot move to `completed` until this task completes.

The branch is based on governed `main` at `f5f6e2b67b07197d531bf9055f82150a8ab0fd9b`. Independent review is required before this task can become `ready-to-merge`.

## Scope

`src/mapping/apply.js`, specifically `setJsonPointer` (array/container creation loop, roughly lines 17–38 as of `13e9262`).

Focused regression coverage in `test/mapping.test.js`, the adapter-contract and `compact` documentation corrections, and governance bookkeeping for this task/phase are also in scope.

## Non-Goals

- No schema redesign (`src/schemas/mapping.schema.json` unchanged).
- No expression AST changes (`src/expressions/evaluate.js` unchanged) — this is not an expression-evaluator bug, it's in the mapping executor's path-writing logic.
- No sandboxing framework or new dependency — the fix is a bounds check, not new infrastructure.

## Acceptance Criteria

- Writing through a numeric array-index path segment that is out of bounds (i.e. not `0 <= index <= currentArray.length`, following RFC 6902 JSON Patch "add" semantics: sequential append or in-bounds replace only) throws instead of silently succeeding.
- All existing tests continue to pass (39/39 as of `P01-T004`).
- A new regression test exists asserting the large-index case throws (e.g. `targetPath: "/arr/999999999/x"`).
- Normal dense writes, existing-element traversal/replacement, and append-at-length continue to work.
- Prototype-sensitive path protections remain intact.
- Sequential calculated fields can read target values produced by earlier calculated fields.

## Implementation Notes

Implemented originally at `c40dd430cc1a5f5904ba3c90a888341cf29c14fd`. The dedicated task worktree rebased the branch onto governed `origin/main` at `f5f6e2b67b07197d531bf9055f82150a8ab0fd9b` without conflicts. `git range-diff` reported the implementation patch unchanged; the rebased implementation commit is `14a66f1`.

The fix matches the recommended approach exactly: a new `resolveArrayIndex(array, segment)` helper rejects non-array-index segments, non-safe-integer indices, and any index `> array.length` (`RangeError`, message includes `"exceeds current length"`), and rejects an in-bounds index that doesn't reference an existing element (sparse-gap guard). A new `assignContainerValue` helper routes array writes through `push` (when index equals current length) or in-place replacement (when index is a valid existing element), instead of raw bracket assignment. This is applied both to the final-segment write and to intermediate container traversal in `setJsonPointer`.

Also included: a `docs/knowledge/extension-workbench.md` note that `compact` follows `filter(Boolean)` truthiness (removes `0`/`false`/`""` along with null/missing), and a `docs/knowledge/adapter-contracts.md` correction to `Source Provider / Adapter -> Source Contract -> Mapping Runtime -> Named Target`. The adapter document clarifies that a deterministic, documented, target-validated adapter may emit an already-normalized named target directly; downstream consumers still depend on the named target contract.

## Files/Modules Expected

- `src/mapping/apply.js`
- `test/mapping.test.js`
- `docs/knowledge/adapter-contracts.md`
- `docs/knowledge/extension-workbench.md`
- `docs/tasks/P01/P01-T007.md`
- `docs/project/implementation-plan.md`
- `docs/project/last-worked-on.md`

## Tests Required

`npm test` must remain green; add the regression test described in Acceptance Criteria.

**Observed after rebase:** `npm test` passed 47/47. Regression coverage proves dense array creation, traversal/replacement of existing elements, append at `array.length`, rejection above `array.length`, rejection of pre-existing sparse holes, immediate rejection of `/arr/999999999/x`, preserved prototype-pollution protections, and sequential calculated-field chaining through the `target` root.

Manual reproduction with `/arr/4000000000/y` threw `RangeError: Array target path index 4000000000 exceeds current length 0.` The target array remained at length `0`; the final observed run completed in `1.641 ms`.

`git diff --check origin/main`: passed.

## Documentation Required

Updated `docs/knowledge/adapter-contracts.md`, `docs/knowledge/extension-workbench.md`, this canonical task record, the P01 phase pointer, and `docs/project/last-worked-on.md`. No ADR is required because this is a security correction within ADR 0012's accepted mapping contract rather than a new architectural decision.

## Security/Compatibility Considerations

This task exists entirely because of a security finding. See Why This Task Exists. No compatibility concern — bounding array indices only rejects inputs that were already pathological; no legitimate mapping project should rely on sparse, far-out-of-bounds array indices.

## Review Findings

Claude's independent review of `P01-T004` returned "APPROVE WITH FIXES" and documented this as the blocking P1 finding. The fix directly implements the recommended append-or-in-bounds semantics. Independent review of the rebased fix branch is still pending; the implementing agent's validation does not satisfy that requirement.

## Result

Implementation and governance integration are complete on `fix/mapping-array-hardening`. The security fix is patch-identical to original commit `c40dd430cc1a5f5904ba3c90a888341cf29c14fd`, all 47 tests pass, and the large-index reproduction fails immediately without array amplification.

**Final status:** `review` — ready for independent review, not approved for merge, and not completed.

## Branch And Merge Lifecycle

- **Implementation commit(s):** `c40dd430cc1a5f5904ba3c90a888341cf29c14fd` (original); `14a66f1` (rebased, patch-identical implementation on the current branch).
- **Fix commit(s):** This task is itself the fix for the `P01-T004` security finding; no additional review-fix commit yet.
- **Merge approval status:** not requested; not approved; independent review pending.
- **Merge commit:** pending.
- **Post-merge validation:** pending.

## Completion Date

Pending. Set only after approved merge and post-merge validation.

## Follow-Up Tasks

After independent review and explicit merge approval: merge the branch, rerun `npm test` on `main`, record the merge commit, mark this task `completed`, close the P01 phase gate, and only then activate P02 on `main`.

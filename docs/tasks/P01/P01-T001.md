# P01-T001: Scalable Extension Model

- **Task ID:** `P01-T001`
- **Phase:** `P01` — Platform Mapping Foundation (see [implementation plan](../../project/implementation-plan.md))
- **Status:** completed
- **Owner:** thehappycamper (with coding-agent assistance)

## Objective

Introduce the extension manifest schema and taxonomy (`emulator`, `game`, `data`, `mechanics`, `ruleset`, `analysis`, `overlay-panel`, `transport`) so future game/emulator/data/UI support can be added without hardcoding every case into core.

## Why This Task Exists

Backfilled from git history. Recorded in `docs/decisions/0005-flexible-extension-model.md`.

## Dependencies

`P00-T001` (static MVP) — this extends the schema/docs foundation it established.

## Scope

Per `git show --stat 7f43625` (18+ files): `docs/decisions/0005-flexible-extension-model.md`; `docs/knowledge/{extension-types,ui-extension-model,custom-data-and-fusions,domain-model}.md`; `docs/knowledge/adapter-contracts.md` update; `docs/project/configuration.md` (new — `.env` policy) and `.env.example` (new); example extension manifests under `examples/extensions/`; `docs/project/{community-extensions,features,last-worked-on}.md`, `docs/maps/code-map.md`, `README.md`, `docs/README.md` updates.

## Non-Goals

No extension runtime/loader (still not implemented as of this writing — see `docs/project/features.md`). No mapping/calculated-field model yet (`P01-T004`).

## Acceptance Criteria

Extension manifests validate as JSON (formal schema validation wasn't automated until `P01-T004`).

## Implementation Notes

n/a — backfilled record.

## Files/Modules Expected

See Scope.

## Tests Required

None added in this commit; extension examples were not schema-validated by `npm test` until `P01-T004`.

## Documentation Required

See Scope — this task is primarily documentation/schema.

## Security/Compatibility Considerations

Established the "no ROMs/BIOS/saves/secrets" public-safety pattern for extensions, later reinforced with `.gitignore` patterns in `P01-T004`.

## Review Findings

None recorded at the time.

## Result

Completed. Extension taxonomy and manifest schema established; became the foundation for `P01-T002`'s template model and `P01-T004`'s mapping-target-type reuse.

## Commit(s)

- `7f43625` — Add scalable extension model

## Completion Date

2026-08-08

## Follow-Up Tasks

`P01-T002` (workbench/domain/template model).

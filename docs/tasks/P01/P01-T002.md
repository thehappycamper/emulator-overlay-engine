# P01-T002: Workbench, Domain Model, Templates, And Licensing

- **Task ID:** `P01-T002`
- **Phase:** `P01` — Platform Mapping Foundation (see [implementation plan](../../project/implementation-plan.md))
- **Status:** completed
- **Owner:** thehappycamper (with coding-agent assistance)

## Objective

Define the Extension Workbench concept, the domain-neutral-core-with-Pokemon-first-domain decision, the shareable-template model, and the repository's MIT/commercial-model policy.

## Why This Task Exists

Backfilled from git history. Recorded in five ADRs: `0006` (Extension Workbench as authoring layer), `0007` (Pokemon-first, domain-extensible — later superseded), `0008` (domain-neutral core, Pokemon as first domain — supersedes `0007`), `0009` (shareable templates), `0010` (MIT core and commercial services).

## Dependencies

`P01-T001` (extension model) — templates bundle extensions.

## Scope

Per `git show --stat cf12434`, `213187d`, `d3b876a`:
- `docs/decisions/0006` through `0010`.
- `docs/knowledge/{architecture,domain-extensibility,extension-workbench,shareable-templates}.md`.
- `docs/project/commercial-model.md` (new).
- `docs/project/{community-extensions,features,last-worked-on,roadmap}.md`, `docs/maps/code-map.md`, `README.md`, `docs/README.md` updates.
- `AGENTS.md` update (Product Direction section introduced).

## Non-Goals

No template *schema* yet — `src/schemas/template.schema.json` did not exist until `P01-T003`/`fe435dc`. This task was documentation/ADR only.

## Acceptance Criteria

ADRs 0006–0010 accurately describe the decisions; `0007` correctly marked superseded by `0008` at authoring time (verified — not a later fix).

## Implementation Notes

n/a — backfilled record.

## Files/Modules Expected

See Scope.

## Tests Required

None (documentation/ADR only).

## Documentation Required

See Scope.

## Security/Compatibility Considerations

ADR 0010 establishes the MIT license as covering shared platform code, domain packages, and accepted community extensions/templates — the licensing baseline all later work operates under.

## Review Findings

None recorded at the time.

## Result

Completed. Established the domain-neutral-core/Pokemon-first-domain direction (`P02` later executes the domain-boundary extraction this decision calls for), the template concept later formalized as a schema in `P01-T003`, and the MIT licensing policy.

## Commit(s)

- `cf12434` — Define workbench templates and domain modules
- `213187d` — Document repository license
- `d3b876a` — Document MIT and marketplace model

## Completion Date

2026-08-08

## Follow-Up Tasks

`P01-T003` (rename), `P02` (domain boundary extraction, planned).

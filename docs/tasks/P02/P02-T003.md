# P02-T003: Domain Registry Descriptor Immutability Hardening

- **Task ID:** `P02-T003`
- **Phase:** `P02` — Domain Boundary (see [implementation plan](../../project/implementation-plan.md))
- **Status:** completed
- **Owner:** Claude (initial implementation); Codex (independent-review fixes); Claude (independent re-verification and three small additional regression tests)
- **Branch:** `fix/P02-T003-domain-registry-immutability`
- **Worktree/Clone Isolation:** Initial implementation used `../emulator-overlay-engine-worktrees/fix-P02-T003-domain-registry-immutability/`. Independent review and focused fixes used the separate clone `../EOE-P02-T003-review`. Independent re-verification synced the original implementation clone (`../emulator-overlay-engine-worktrees/fix-P02-T003-domain-registry-immutability/`) to the force-pushed remote tip via `git reset --hard` after confirming a clean working tree. Independent re-review and approved integration used a fresh detached review clone at `../EOE-review-P02-T003-rereview`; none of these shared a working tree with another active task.

## Objective

Make domain-descriptor immutability a guarantee the platform registry (`src/platform/domain-registry.js`) enforces itself, rather than a convention each domain package happens to follow.

## Why This Task Exists

Closes the non-blocking finding from Claude's independent review of `P02-T001` (recorded in `docs/tasks/P02/P02-T001.md`'s Review Findings): `createDomainRegistry` freezes only its own `has`/`resolve` interface object, never the domain descriptor objects passed into it. Independently verified during that review — registering an unfrozen test domain and mutating a calculator on it *after* registration caused `resolve()` to return the mutated version. Harmless today because the only registered domain (`pokemonDomain`) self-freezes and there is no dynamic/untrusted domain-loading path yet, but this should be closed before a second domain or any dynamic loading is introduced.

## Dependencies

`P02-T001` — completed, merged to `main` at `2617f4a`.

Independently parallelizable with `P02-T002` — disjoint files (this task touches only `src/platform/domain-registry.js` and its tests; `P02-T002` touches schema/fixture paths). Confirm that remains true before running them concurrently, per `AGENTS.md`'s Parallel Task Planning rule.

## Scope

- `src/platform/domain-registry.js`: recursively freeze each registered domain descriptor at registration time, bounded to plain-object/array containers only (functions and other non-container values are left untouched).
- `test/platform-domain-registry.test.js`: the original nine adversarial tests plus focused regressions for partially frozen graphs, multi-object cycles, repeated references, rejected class/custom-prototype roots, accepted null-prototype roots, and plain-descriptor identity.
- `docs/knowledge/domain-extensibility.md`: one paragraph recording the registry's immutability guarantee.

## Non-Goals

- No change to `src/domains/pokemon/*` — `pokemonDomain` already self-freezes correctly; this task hardens the platform side, not the existing domain.
- No dynamic/runtime domain loading, no domain-descriptor schema, no second domain — those remain separate future work per `P02-T001`'s Non-Goals.
- No change to the mapping runtime or expression evaluator — unrelated surface.

## Acceptance Criteria

- ✅ A domain object registered without being pre-frozen by its own package cannot have its fields mutated (directly or via a caller-held reference) after registration in a way that changes what `resolve()` returns. Verified at the top level, one level of nesting (`calculators`), and three levels of nesting (`metadata.nested.deeper.value`), plus mutation attempted directly through the resolved reference.
- ✅ Existing `pokemonDomain` behavior and all currently-passing tests remain green — 73/73 after merge on `main`, including both identity-sensitive `P02-T001` tests.
- ✅ The new regression tests fail against the pre-`P02-T003` code and pass after the fix — verified explicitly by temporarily reverting the registry change and rerunning the new test file.
- ✅ Already-frozen roots and intermediate containers no longer hide mutable descendants; graph traversal uses explicit visited-state rather than frozen-state cycle detection.
- ✅ Top-level descriptors must be plain objects; `Object.prototype` and null-prototype objects are accepted, while class instances and custom-prototype objects fail deterministically.

## Implementation Notes

**Design decision — freeze-in-place vs. defensive clone.** Two strategies were considered:

1. **Freeze the caller's descriptor tree in place** (chosen). Simple, no cloning cost, and — critically — preserves the strict object-identity guarantees `P02-T001`'s own tests already depend on: `test/domain-boundary.test.js` asserts `assert.strictEqual(resolveDomain("pokemon"), pokemonDomain)` (the exact same top-level object) and `assert.strictEqual(legacyCalculateCaptureChance, pokemonDomain.calculators.calculateCaptureChance)` (the exact same function reference, proving the `src/engine/*` compatibility shim is a true re-export, not a duplicate implementation). Freezing in place keeps both of those true by construction — the registry stores and returns the identical reference it was given, just no longer mutable.
2. **Deep-clone into an internally-owned structural copy, freeze the copy.** Rejected: this would make `resolveDomain("pokemon") !== pokemonDomain` (a different, cloned object), breaking the existing `strictEqual` test above. Changing that test's semantics was out of scope for this task and would have meant relitigating an already-reviewed, already-merged behavioral guarantee from `P02-T001`.

Freezing an object the caller still holds a reference to is, strictly speaking, a mutation of caller-owned state (per the task's "do not mutate caller-owned objects unexpectedly if avoidable" guidance) — but it's the *documented, intentional point* of this task, not a side effect. It's called out explicitly in the new module comment in `src/platform/domain-registry.js` and in this record, so it's not unexpected/surprising, and for the one real domain (`pokemonDomain`, which already self-freezes) it's a complete no-op in practice — the guarantee only actually starts mutating something the moment a future domain author forgets to self-freeze, which is exactly the case this task exists to cover.

**Design decision — freeze depth.** Rejected two extremes: (a) a shallow `Object.freeze(domain)` only, which the task brief itself flagged as insufficient (`domain.calculators.someCalculator` would still be reachable and mutable); (b) a fully generic deep-freeze framework handling every JS object type, which is more machinery than this boundary needs and could behave surprisingly on object types the registry has no reason to understand (Maps, class instances, etc.). Landed on a bounded recursive freeze restricted to plain objects and arrays — the same "is this JSON-shaped data or something else" distinction `src/mapping/apply.js` and `src/expressions/evaluate.js` already use elsewhere in this codebase (`Object.getPrototypeOf(value) === Object.prototype`) — so the fix is consistent with an established idiom rather than inventing a new one. This freezes however many levels of plain-object/array nesting a descriptor actually has (verified with a 3-level-deep test case), without attempting to freeze functions (which wouldn't meaningfully change their behavior) or exotic objects.

**Circular-reference safety.** The initial implementation incorrectly used `Object.isFrozen()` as both an immutability check and cycle guard. Independent review showed that a shallow-frozen root or intermediate container could therefore hide mutable descendants. The focused fix uses a per-traversal `WeakSet`: eligible containers are marked visited, frozen only when needed, and their children are always traversed on first visit regardless of prior frozen state. Self-cycles, multi-object cycles, and repeated references terminate while preserving identity.

**Top-level descriptor shape.** Domain packages must be structural plain objects. Objects using `Object.prototype` or a null prototype are accepted. Arrays, class instances, and objects with custom prototypes are rejected with `TypeError: Domain packages must be plain objects`. Nested functions and exotic values remain outside the structural-freeze guarantee rather than being generically traversed or frozen.

**Residual recursion-depth limitation.** Traversal remains recursive. Independent review demonstrated that an artificial graph around 20,000 levels deep can exhaust the JavaScript stack. Domains are currently statically imported and repository-reviewed, so this is accepted as non-blocking under the present trust model. Iterative traversal or an explicit depth limit must be reconsidered before untrusted runtime domain installation.

**Verified the tests are meaningful, not padding.** Temporarily reverted the registry fix (`git stash push -- src/platform/domain-registry.js`) and reran the new test file against the original code: multiple tests failed as expected (including the circular-reference and top-level-mutation tests), confirming these are real regression tests. Restored the fix afterward; full suite green again.

## Files/Modules Expected

`src/platform/domain-registry.js`, `test/platform-domain-registry.test.js`, `docs/knowledge/domain-extensibility.md`, `docs/tasks/P02/P02-T003.md`.

## Tests Required

`npm test`: **73/73 passing** (70 after the focused fix, plus 3 added during independent re-verification — see Review Findings). Focused registry test file: **20/20 passing**. `node --check src/platform/domain-registry.js`: passed. `git diff --check`: passed. Confirmed the two identity-sensitive `P02-T001` tests (`Pokemon resolves through the application domain boundary`, `legacy engine imports remain compatible during incremental migration`) still pass. Regressions cover both originally-identified bypasses, multi-object cycles, shared references, null-prototype acceptance, and deterministic rejection of class instances, custom-prototype objects, `Map`, `Set`, and arrays as top-level descriptors.

## Documentation Required

`docs/knowledge/domain-extensibility.md` — one paragraph recording the registry's immutability guarantee, added alongside its existing registry description. `docs/tasks/P02/P02-T003.md` (this record). No ADR — this is a bugfix/hardening within the accepted `P02-T001` direction, not a new architectural decision (same reasoning `P01-T007` used for its own no-ADR call).

## Security/Compatibility Considerations

This task exists entirely because of a defense-in-depth security/robustness finding — see Why This Task Exists. Freeze-in-place remains the explicit ownership contract and preserves descriptor/calculator identity. The guarantee is structural: reachable plain-object/array containers are frozen, while executable behavior, closed-over state, and nested exotic objects remain trusted code. No current code relies on mutating a registered descriptor after registration.

## Review Findings

Independent review returned **REQUEST CHANGES**.

Blocking finding 1: `Object.isFrozen()` was incorrectly used as cycle detection, allowing mutable descendants beneath already-frozen roots or intermediate containers. Fixed with explicit `WeakSet` visitation and regressions for both graph shapes.

Blocking finding 2: class-instance/custom-prototype top-level descriptors were accepted but skipped by the bounded freeze. Fixed by requiring a top-level plain object, explicitly accepting null-prototype objects, and adding deterministic rejection tests.

Non-blocking review findings were also addressed: the platform comment is domain-neutral, documentation now promises structural container immutability rather than arbitrary behavior immutability, and P02-T003 project-status pointers are current.

**Independent re-verification pass (Claude):** found the fix commit (`36b5aaf`) on the (force-pushed, rebased) remote branch already correctly closed both blocking findings — confirmed by tracing the code (the new `isPlainObject`/`isDescriptorContainer`/`WeakSet`-based `freezeDescriptorTree` correctly traverses beneath already-frozen containers and correctly rejects non-plain top-level descriptors) and by independently reproducing both original bypass classes from scratch in a fresh script (not reusing the checked-in tests): a frozen root with a mutable nested child now gets that child frozen too; a class-instance descriptor is now rejected outright. Did not redo or overwrite this work. One real, minor gap found: the review explicitly named `Map`/`Set` as examples of descriptor shapes to reject and test, but the checked-in suite only had a generic custom-prototype rejection test, not literal `Map`/`Set`/array cases. Added three small regression tests (`Map domain descriptors are rejected`, `Set domain descriptors are rejected`, `array domain descriptors are rejected`) closing that literal gap — verified each independently reproduces and passes against the existing (already-correct) implementation.

**Independent re-review (Codex): APPROVE.** Reviewed branch HEAD `7fa074b3a1ee90ac1a6d45cbf1ce96e1f6740122` against approved `main@f05b925cf7f17664121bc78ba2e9ceb1839425cb` from a separate clone. Independently confirmed frozen-parent and frozen-intermediate descendants are traversed/frozen, self/multi-object cycles and repeated references terminate with identity preserved, ordinary/null-prototype descriptors are accepted, and arrays/class instances/custom-prototype objects/`Map`/`Set` are rejected before freeze traversal with the documented deterministic error. Full suite passed 73/73; focused registry suite passed 20/20; syntax and diff checks passed. The documented pathological recursion-depth limitation remains non-blocking under the static, repository-reviewed domain trust model. Merge recommendation: ready for merge approval.

## Result

The platform registry now enforces structural descriptor-container immutability for accepted plain-object domain packages. Explicit graph visitation safely traverses mutable descendants beneath frozen containers and terminates across cycles/shared references. Non-plain top-level descriptors — including class instances, custom-prototype objects, `Map`, `Set`, and arrays — are rejected rather than silently bypassing ownership enforcement. Freeze-in-place and calculator identity remain unchanged.

**Final status:** `completed` — both blocking findings were fixed, independent re-review returned APPROVE, project-owner merge approval was granted, the branch was fast-forwarded into `main`, and post-merge validation passed.

## Branch And Merge Lifecycle

- **Implementation commit(s):** original `555c65a`; rebased onto completed `P02-T002` as `073e8e67a4e3726e212f766da3044d47bf241f2d` (bookkeeping `1559ca46c8b151eede02d569abd3dc7bfbec9e07`).
- **Fix commit(s):** `36b5aafd1713ae6feefd1f8878a5f63719f66aee` (WeakSet traversal + top-level plain-object validation); `e315002` (explicit `Map`/`Set`/array rejection tests, added during independent re-verification). Independent re-review subsequently returned APPROVE.
- **Merge approval status:** approved — explicitly granted by the project owner after independent re-review returned APPROVE.
- **Merge commit:** `7fa074b3a1ee90ac1a6d45cbf1ce96e1f6740122` — integrated by fast-forward from `f05b925cf7f17664121bc78ba2e9ceb1839425cb`, preserving all task commits.
- **Post-merge validation:** On integrated `main`: `npm test` 73/73 passing; `node --check src/platform/domain-registry.js` passed; `git diff --check` passed; independent script confirmed the frozen-parent/mutable-child and non-plain-descriptor bypasses remain closed; `resolveDomain("pokemon") === pokemonDomain`, legacy calculator identity, and calculator behavior remain intact.

## Completion Date

2026-08-08

## Follow-Up Tasks

None yet known.

# P02-T004: Record Progress, Spatial, And Cross-Domain Product Direction

- **Task ID:** `P02-T004`
- **Phase:** `P02` — Domain Boundary (see [implementation plan](../../project/implementation-plan.md))
- **Status:** completed
- **Owner:** Codex
- **Branch:** `docs/product-vision-progress-spatial-cross-domain`
- **Worktree/Clone Isolation:** independent clone at sibling directory `../EOE-product-vision-progress-spatial-cross-domain`; no active agent shares this working tree

## Objective

Preserve future product direction for internal automation ownership, external workflow interoperability, normalized progress and results, progression graphs, spatial/map capabilities, cross-domain competition, and Pokemon composition so a contributor can reconstruct it without chat history.

## Why This Task Exists

The canonical vision already established source-agnostic state, future semantic events/actions, sessions, and optional hosted services. It did not yet define clear ownership boundaries for workflow systems, progress, maps, scoring/results, or cross-domain rules. This task records those boundaries before later implementation choices accidentally narrow the platform.

This documentation-only task is assigned to the active `P02` phase because repository governance requires every task to belong to exactly one phase. It is not a `P02` runtime deliverable, does not change the phase exit gate, and does not activate later product tracks.

## Dependencies

- Current approved `main` at task start: `f05b925`.
- ADR 0013 for future semantic events, ADR 0014 for session/referee direction, and ADR 0015 for Pokemon composition/state ownership.
- No runtime task depends on this branch before it is approved and merged.

## Scope

- Expand the canonical product vision with direction-only automation, progress, spatial, scoring/result, and cross-domain concepts.
- Add a lightweight direction ADR and clarify ADR 0014's same-revision example.
- Update architecture/domain/Workbench summaries, feature inventory, roadmap, implementation-plan pointer, architecture graph, and project memory.
- Audit active documentation for contradictions against the recorded direction.

## Non-Goals

- No runtime code, schemas, mappings, tests, progress engine, progression graph implementation, map system, rules engine, event/action/session runtime, integration provider, scoring implementation, or cloud service.
- No public identifier, event name, action name, coordinate model, progress formula, denominator, result schema, or rating algorithm.
- No n8n, Node-RED, Home Assistant, webhook, WebSocket, MQTT, IPC, or inbound Action API implementation.
- No Pokemon generation/game/revision package hierarchy implementation.

## Acceptance Criteria

- [x] `docs/knowledge/product-vision.md` is the canonical detailed record and clearly marks all added capabilities as future direction.
- [x] Internal EOE semantic/rule/action ownership and optional external workflow interoperability are distinguished.
- [x] Progress, progression graphs, spatial maps, session results, cross-domain objectives, and session score versus player rating are distinguished without premature schemas.
- [x] Pokemon's composition model remains one domain plus generation/mechanics, game configuration, and revision/mod overrides.
- [x] Active-documentation consistency audit finds no unresolved contradictory claims.
- [x] `npm test`, markdown-link validation, and `git diff --check` pass.
- [x] Branch contains documentation only and is ready to push without merging.

## Implementation Notes

ADR 0016 records the architectural boundaries but deliberately leaves every public contract and implementation decision deferred. ADR 0014's historical wording remains intact; a clarification marker and ADR 0016 explain that identical game/revision requirements apply only when a selected ruleset requires them.

Maps and progression graphs are explicitly separate: maps describe spatial placement, while progression graphs describe prerequisites and reachability. Cross-domain objective translation is assigned to rules/session logic so source providers and domain events retain their own meaning.

The consistency audit corrected two narrow/ambiguous active descriptions: ADR 0014's same-revision example now points to the ruleset-specific clarification in ADR 0016, and ruleset summaries no longer imply every challenge requires numeric scoring. Searches found no active claim that progress requires manual entry, all games share one formula/coordinate model, maps are Pokemon-only, external workflow tools replace the internal engine, multiplayer requires identical games, one Pokemon game equals one domain, or session score equals player rating.

## Files/Modules Expected

- `docs/knowledge/{product-vision,architecture,domain-extensibility,extension-workbench}.md`
- `docs/decisions/0014-session-referee-and-verified-environments.md`
- `docs/decisions/0016-progress-spatial-and-cross-domain-experience-direction.md`
- `docs/project/{features,implementation-plan,roadmap,last-worked-on}.md`
- `docs/graph/architecture.mmd`
- `docs/README.md`
- `docs/knowledge/extension-types.md`
- `extensions/README.md`
- `rulesets/README.md`
- `docs/tasks/P02/P02-T004.md`

## Tests Required

- `npm test` — 54/54 passing. Initial sandboxed attempt could not spawn Node test workers (`EPERM`); the required unrestricted rerun passed.
- Repository-wide relative Markdown-link validation — 79 Markdown files checked, no broken relative links.
- Vision consistency searches — no unresolved contradictory active descriptions; two corrections recorded in Implementation Notes.
- `git diff --check` — passed.
- Full diff/file-scope review against `origin/main` — documentation Markdown and Mermaid only; no runtime, schema, test, dependency, or CI changes.

## Documentation Required

This task's entire deliverable is documentation. Canonical detail belongs in `docs/knowledge/product-vision.md`; other files summarize or link to it.

## Security/Compatibility Considerations

No runtime or public contract changes. The direction requires future inbound external actions to pass authentication/authorization, session policy, and provider-capability checks. It also prevents external workflow systems from becoming a bypass around platform-owned safe execution.

## Review Findings

Independently reviewed against approved `main` (base `f05b925`, branch HEAD `192a84b`, implementation commit `97228a8`) prior to merge. Verdict: **APPROVE**, no blocking findings. The review confirmed: the automation-interoperability pipeline, normalized-progress-not-universal-formula framing, progression-graph-vs-spatial-map distinction, cross-domain session/scoring boundaries, and Pokemon composition model all match the required product direction without freezing schemas, formulas, identifiers, or runtime commitments, and without implying anything is implemented; ADR 0016 correctly records direction only with an explicit Non-Decisions section; the ADR 0014 clarification marker is genuinely additive (one header line, zero body text changed); `docs/tasks/P02/P02-T004.md` correctly justifies this task's placement in the active `P02` phase as non-gating; a repository-wide implemented-vs-planned grep found no violations; `npm test` (54/54 on the branch's own base), `git diff --check`, and a full relative-link check all passed independently; the branch's diff against its own base touches documentation/Mermaid only. One non-blocking note: `main` had advanced past this branch's merge-base in the interim (`P02-T003` merged), producing real content overlap in `docs/project/implementation-plan.md` and `docs/project/last-worked-on.md` requiring manual 3-way reconciliation during merge — not a content defect in this branch, and reconciled as part of the merge commit below.

## Result

The canonical vision now records the approved future boundaries for internal automation ownership, optional workflow interoperability, progress/results, progression graphs, spatial/map capabilities, cross-domain rules/objectives, score versus rating, and Pokemon composition. Concise inventories and project memory link back to that direction. No behavior or public contract changed.

## Branch And Merge Lifecycle

- **Implementation commit(s):** `97228a890c28cf78c4b39f4e65c5f759d5f17d63` — documentation, ADR, consistency corrections, project memory, and validation record.
- **Fix commit(s):** none.
- **Merge approval status:** granted (independent review APPROVE, project-owner conditional merge authorization).
- **Merge commit:** `c761820` on `main` — `main` had advanced past this branch's base in the interim (`P02-T003` merged at `dc79e2a`), so a non-fast-forward merge with manual reconciliation of `docs/project/implementation-plan.md` and `docs/project/last-worked-on.md` was required; `docs/knowledge/domain-extensibility.md` auto-merged cleanly. Both branches' true content (P02-T003's completion status and P02-T004's product-direction content) is preserved.
- **Post-merge validation:** `npm test` 73/73 passing; `git diff --check` clean; repository-wide relative Markdown-link check clean (37 links checked, 0 broken).

## Completion Date

2026-08-08

## Follow-Up Tasks

Future implementation tasks must be separately scoped only after prerequisite event/rule/action/session contracts exist. This task does not create those tasks or assign implementation dates.

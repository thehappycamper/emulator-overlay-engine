# P06-T002: Provider-Neutral Emerald Acquisition

- **Task ID:** `P06-T002`
- **Phase:** `P06` - Second Source / Domain Proof (see [implementation plan](../../project/implementation-plan.md))
- **Status:** review
- **Owner:** Codex
- **Branch:** `feat/P06-T002-provider-neutral-emerald-acquisition`
- **Worktree/Clone Isolation:** dedicated independent clone; no shared active-agent working tree

## Objective

Establish provider-neutral ownership for Pokemon Emerald English retail Rev 0 acquisition and make BizHawk emit that contract through the already proven mapping, Pokemon state/domain, atomic live-state handoff, and overlay.

## Why This Task Exists

`P06-T001` proved BizHawk startup and identity/frame APIs but correctly refused to emit the mGBA-named source contract. Shared Emerald addresses and Gen III decoding were still owned by `adapters/gen3-mgba/`, which would force duplication or false provenance for a second provider. ADR 0020 records the corrected boundary.

## Dependencies

- `P06-T001` completed.
- Existing P05 source validation, safe mapping, Pokemon target validation, atomic `public/live-state.json` handoff, and overlay polling remain unchanged downstream.
- Real-ROM acceptance depends on the operator's legally obtained Emerald Rev 0 image and local BizHawk installation.

## Scope

- Move Emerald identity, addresses, Gen III decoding, contract/schema, fixtures, mapping, and Node orchestration into `adapters/pokemon-emerald-us-rev0/`.
- Add a shared Lua acquisition module loaded by mGBA and BizHawk providers.
- Add provider provenance to the new source contract.
- Adapt BizHawk local configuration and connector to emit the shared source snapshot.
- Verify BizHawk System Bus reads against direct EWRAM/IWRAM offsets before publication.
- Retain mGBA JavaScript compatibility entry points.

## Non-Goals

C# External Tool, second game/domain, whole-party iteration, lookup data, semantic events/actions, WebSockets, cloud, or Proof 2 acceptance without a real-ROM session.

## Acceptance Criteria

- Contract identity does not encode an emulator provider.
- Emerald addresses and Gen III decoding are not duplicated between mGBA and BizHawk providers.
- Both providers preserve useful provenance and can satisfy the same schema.
- BizHawk fails closed on unsupported version/system/hash, domain shape, read mismatch, or invalid acquisition.
- One existing declarative mapping produces validated Pokemon state for both providers.
- Full tests and repository validation pass.
- Branch is pushed and stops for independent review.

## Implementation Notes

The chosen contract is `pokemon.emerald.us-rev0.acquisition@1.0.0`. `source` records provider identity, Lua integration, common GBA address-space semantics, primary provider domain/API, and empirically verified direct domains. This is narrow contract-specific provenance, not a universal provenance/confidence model.

BizHawk 2.11.1 upstream source defines `System Bus` (`0x10000000`), `EWRAM` (`0x40000`), and `IWRAM` (`0x8000`). The connector checks those dimensions, then compares each known absolute System Bus read with the translated direct WRAM read of the same width. This runtime check supplies empirical evidence only when the real operator session runs.

## Files/Modules Expected

- `adapters/pokemon-emerald-us-rev0/**`
- `adapters/gen3-mgba/**`
- `adapters/bizhawk/**`
- `tools/{bizhawk-proof-config,emerald-proof-config,launch-bizhawk-proof,emerald-live-state}.mjs`
- focused tests and affected architecture/project documentation

## Tests Required

- `npm test`: 142/142 passing.
- Provider parity from equivalent synthetic memory: passing.
- Existing downstream mapping provider independence: passing.
- Mapping schema/source schema/extension manifests: passing in repository schema validation.
- `git diff --check`: clean. JavaScript syntax checks: pass. Relative Markdown links: 65 checked, 0 broken.
- Fixture-to-`public/live-state.json` orchestration equivalent: one-shot temporary-path smoke passed (`mGBA`, species `258`, HP `31/35`).
- Local launcher path validation is covered by deterministic tests; the real local `--check` and emulator launch remain part of operator acceptance because machine-specific config is not committed.
- Real BizHawk ROM smoke: pending operator execution; must not be inferred from fixtures.

## Documentation Required

ADR 0020, adapter READMEs, adapter contracts/architecture, code map, feature/emulator/configuration inventories, roadmap/implementation plan, and project memory.

## Security/Compatibility Considerations

Unsupported identities and inconsistent memory fail closed and remove the configured source snapshot. No game assets or machine paths are committed. Existing mGBA JavaScript import paths are retained as compatibility modules; the old mGBA-named JSON contract ID remains historical and is not silently reinterpreted.

## Review Findings

Independent review is required because this changes a public source contract and adds a second live-memory provider. Review is pending. Merge approval is pending.

## Result

Implementation is complete on the task branch. Provider-neutral contract emission and synthetic downstream parity are proven. Proof 2 remains incomplete until the real BizHawk operator session records live HP plus one battle/location change and confirms the domain-parity checks do not fail.

## Branch And Merge Lifecycle

- **Implementation commit(s):** pending branch commit
- **Fix commit(s):** none
- **Merge approval status:** not requested
- **Merge commit:** pending
- **Post-merge validation:** pending

## Completion Date

Pending.

## Follow-Up Tasks

- Record the real BizHawk Proof 2 operator acceptance session.
- Scope broader P05/P06 acquisition only after acceptance evidence identifies the next concrete gap.
